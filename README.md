# DataScience #audiodataset
1)Using the generated texture of audio signals, classify the audio signals with any deep transfer learning Keras architecture (for example, vgg, resnet,.....) and measure the following performance metrics: classification accuracy and F1 score.
2) Using the generated texture, measure the classification accuracy and F1 score with and without transfer learning on a CNN deep architecture, and check is there any negative transfer learning occurred.
